apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-violation-fixture
spec:
  replicas: 1
  selector:
    matchLabels:
      app: policy-violation-fixture
  template:
    metadata:
      labels:
        app: policy-violation-fixture
    spec:
      containers:
        - name: app
          image: example.invalid/app:latest
          securityContext:
            privileged: true
